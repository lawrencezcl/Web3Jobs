export const config = {
  lever_companies: ['ledger'],
  greenhouse_boards: ['ripple'],  // Removed consensys (0 jobs)
  enable_remoteok: false,  // Disabled - API not responding
  rss_feeds: [] as string[]  // Removed - RSS feed returning 404
}
